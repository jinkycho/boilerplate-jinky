import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2BEF57",
    },
    secondary: {
      main: "#040828",
    },
    error: {
      main: "#FF8989",
    },
  },
});

export default theme;
